# SESSION_HANDOFF — houjin-navi（法人ナビ）

次セッションはこのファイルを最初に読んでください。

## 0. 最初に読むファイル（順序）
1. `SESSION_HANDOFF.md`（本ファイル）
2. `PROGRESS.md`（進捗・DoD・フェーズ履歴）
3. `ログ/2026-07-13_houjin-navi-セッションログ.md`（直近セッションの詳細・v2.2〜v2.4）／必要なら `ログ/2026-07-12_...`（v1.0〜v2.1）
4. `CLAUDE.md`（プロジェクト規約・不変条件）／`CONSTRAINTS.md`
5. 実装: `src/data.js`（コンテンツ・診断定義）→ `src/app.js`（ロジック・UI）

## 1. 現在の状態（実測 2026-07-13・セッション終了時）
- ブランチ: `main`（`origin/main` と一致・差分0・**未コミット変更なし**）。v2.2〜v2.4 すべて push 済み・本番反映済み。
- 最新コミット（HEAD）: `fdb7ab8`（v2.4 診断重み微調整）。機能コミット: `e569af9`(株式) `78d0e09`(一般社団) `fdb7ab8`(診断)。
- 単体テスト: `node --test` → **44 pass / 0 fail**
- 本番: https://houjin-navi.kei-love-snow2.workers.dev （HTTP 200・v2.4 反映済み）
- GitHub: https://github.com/keilovesnow2-design/houjin-navi （push で自動デプロイ）
- チェックポイントtag: **`checkpoint-2026-07-13`**（本セッション完成状態＝v2.4／`git checkout checkpoint-2026-07-13` で復元可）／`checkpoint-2026-07-12`（v2.1）

## 2. 完成している機能
- 理想ドリブン法人診断（10問4セクション／個人事業・合同・株式・一般社団）
- 相性=「その道への近さ」(満点比・100%到達可能)／強みを先に褒める／近づくヒント(上位理想のみ)／必要な準備(出典付き)／what-if(結果で回答変更)／「大丈夫・安心してください」を要所配置
- 探索診断モード／診断スキップ直行
- **手続きナビ フル対応: 個人事業・合同会社・株式会社・一般社団法人（4タイプすべて）**。各タイプでステップ／基本情報／書類ドラフト自動生成／届出期限計算＋ICS に対応。
  - 株式会社（v2.2）: 書類7種（定款〜設立登記申請書）。登録免許税 最低15万・定款認証3〜5万。
  - 一般社団法人（v2.3・未push）: 書類6種。登録免許税6万定額・定款認証5万・印紙不要。

## 3. 次セッションの最優先アクション（優先順）
- **全機能push済み・未着手の必須タスクなし**。以下はすべて任意。
1. （任意）自治体別の届出期限DB拡充（現状は全国共通＋「自治体で要確認」表示）。
2. （任意）一般社団の「非営利型／収益事業の有無」で税務（青色・均等割）の扱いが変わる点の精緻化。
3. （任意）診断のさらなる微調整。**注意**: ランキングは `scoreDiagnosis` の `match`（score/typeMax の％）順（app.js:79）。生スコア順ではない点を前提に検討すること。

## 4. やってはいけないこと
- 数値・期限を出典なしで `data.js` に書かない（CONSTRAINTS C-02）。掲載はA/B二源以上一致のみ。
- 外部送信コード（fetch/CDN/解析タグ）を追加しない（C-01）。
- 免責・最終確認日の常時表示を外さない（C-03/C-04）。
- 入力/クリック中に対象を含むDOMを全体再描画しない（FP-001／差分更新で）。
- `Desktop/指示書.md`（6/26・別プロジェクト）を上書きしない。

## 5. 品質ゲート
- 変更後: `node --test` 緑 → ブラウザで `src/index.html` を開き診断→結果→フローを目視 → `git push`（自動デプロイ）→ 本番で相性計算・出典・免責を確認。

## 6. 更新の作法
- 制度・診断は原則 `src/data.js` のみ編集。`META.lastVerified` と `source` を更新。
