# CONSTRAINTS.md — 不変条件（houjin-navi）

## 1. 絶対に守る（違反=Red）
- C-01: **外部送信禁止**。fetch/XHR/WebSocket/解析タグ/外部CDN読み込みを一切書かない。個人情報はlocalStorageのみ（SEC-001）。
- C-02: **数値・期限は出典必須**。data.js に載せる制度数値は本リサーチでA/B二源以上一致のもののみ。各項目に `source` を持たせる（RISK-001, NFR-FUNC-001）。
- C-03: **免責の常時表示**。全画面に「情報提供であり法的助言・登記代理でない／最終確認は公式・専門家へ」を表示。初回同意必須（FR-SAFETY-001）。
- C-04: **最終確認日 `META.lastVerified` を表示**し、制度改正で変わりうる旨を明記。
- C-05: 生成書類は必ず「ドラフト・要最終確認」注記付き。提出可能物と誤認させない。
- C-06: 破壊的操作は「データ全消去」のみ。実行前に確認ダイアログ（DRY_RUN思想）。

## 2. スコープ境界
- C-07: 対象は合同会社・金銭出資・社員1名の標準ケースのみ。現物出資/法人社員/許認可業種/株式会社は対象外と明示。

## 3. 実装規約
- C-08: 依存ゼロ（フレームワーク・ビルド・CDN禁止）。`index.html`直開きで動く。
- C-09: 純粋関数は `window.HN` にexportし、`typeof module!=='undefined'`ガードでNodeテスト可能に。
- C-10: コンテンツ(data.js)とロジック(app.js)を分離。制度データのハードコードをapp.jsに書かない。

## 4. 失敗→禁止事項（運用中に追記）
- C-11 (FP-001): 入力・クリック等の進行中操作の最中に、その操作対象を含むDOMを作り直さない。表示更新は差分で行う（全体render禁止）。
